# UniFi Protect Viewport

One instance per Protect Viewport, bound to the UniFi Protect Gateway
(SBOS). Commands: Set Live View, Next/Previous Live View, and **Show View
Temporarily** - show a view for N seconds, then restore whatever was up
(the doorbell-to-TV move; overlapping temporary shows keep the ORIGINAL
restore target). Available views are discovered from the console.
